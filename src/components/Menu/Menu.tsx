import React from 'react';
import classes from './Menu.module.scss'
const Menu = () => {
    return (
        <nav className={classes.menu}>
            МЕНЮ
        </nav>
    );
};

export default Menu;